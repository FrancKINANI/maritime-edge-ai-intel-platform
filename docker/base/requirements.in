# Base requirements — shared across all platform microservices
# This file is compiled to requirements.txt via:
#   uv pip compile --python-version 3.11 --upgrade requirements.in -o requirements.txt

# API layer (every service exposes REST endpoints)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Validation (shared schemas)
pydantic>=2.7.0

# HTTP client (for inter-service calls)
httpx>=0.28.0

# Data processing
numpy>=1.26.0
