[pytest]
pythonpath = .
testpaths = services/ shared/ research/ tests/
addopts = --import-mode=importlib
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: integration / end-to-end tests
