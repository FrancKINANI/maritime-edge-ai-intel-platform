[pytest]
pythonpath = .
testpaths = services/ shared/ phase0/ tests/
addopts = --import-mode=importlib
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: integration / end-to-end tests
